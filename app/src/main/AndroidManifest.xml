<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.gmo.samplerectactive">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".operators.Example1Activity" />
        <activity android:name=".operators.Example2Activity" />
        <activity android:name=".operators.DebounceOperatorActivity" />
        <activity android:name=".operators.Example3Activity" />
        <activity android:name=".operators.Example4Activity"></activity>
        <activity android:name=".operators.MapOperatorActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".operators.ConcatOperatorActivity"></activity>
        <activity android:name=".operators.FlatMapOperatorActivity"></activity>
    </application>

</manifest>